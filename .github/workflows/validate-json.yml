name: Validate locations.json

on:
  push:
    branches:
      - main
    paths:
      - locations.json

jobs:
  validate-json:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Validate JSON
        uses: vgaidarji/validate-json-action@v2
        with:
          files: locations.json